@model IEnumerable<HDKmall.Models.Coupon>
@{
    ViewData["Title"] = "Quản lý mã giảm giá";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold mb-0">🎟️ Quản Lý Mã Giảm Giá</h2>
    <a asp-area="Admin" asp-controller="Coupon" asp-action="Create" class="btn btn-danger text-white fw-bold shadow-sm px-4 rounded-pill">
        <i class="bi bi-plus-circle me-1"></i> Thêm Mã Mới
    </a>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show shadow-sm border-0 rounded-3" role="alert">
        <i class="bi bi-check-circle-fill me-2"></i> @TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show shadow-sm border-0 rounded-3" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i> @TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card shadow-sm border-0 rounded-4 mb-4">
    <div class="card-header bg-white border-bottom-0 pt-4 pb-3 px-4 d-flex justify-content-between align-items-center">
        <h5 class="mb-0 fw-bold text-secondary">Tất cả mã giảm giá</h5>
        <form asp-controller="Coupon" asp-action="Index" method="get" class="d-flex" style="width: 320px;">
            <div class="input-group">
                <span class="input-group-text bg-light border-end-0 rounded-start-pill"><i class="bi bi-search text-muted"></i></span>
                <input type="text" name="q" value="@ViewBag.Search" class="form-control bg-light border-start-0 rounded-end-pill" placeholder="Tìm mã giảm giá..." />
            </div>
        </form>
    </div>

    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light text-secondary">
                    <tr>
                        <th class="px-4 py-3 border-0">ID</th>
                        <th class="py-3 border-0">Mã</th>
                        <th class="py-3 border-0 text-end">Giảm giá</th>
                        <th class="py-3 border-0">Hết hạn</th>
                        <th class="py-3 border-0 text-center">Đã dùng / Giới hạn</th>
                        <th class="py-3 border-0 text-center">Trạng thái</th>
                        <th class="px-4 py-3 border-0 text-end">Thao tác</th>
                    </tr>
                </thead>
                <tbody class="border-top-0">
                    @if (Model != null && Model.Any())
                    {
                        foreach (var coupon in Model)
                        {
                            var expired = coupon.ExpiryDate < DateTime.Now;
                            <tr>
                                <td class="px-4 text-muted fw-medium">#@coupon.Id</td>
                                <td>
                                    <span class="fw-bold font-monospace badge bg-light text-dark border px-3 py-2" style="font-size:14px;">@coupon.Code</span>
                                </td>
                                <td class="text-end fw-bold text-danger">@coupon.DiscountAmount.ToString("N0")₫</td>
                                <td>
                                    <span class="@(expired ? "text-danger" : "text-muted") small fw-medium">@coupon.ExpiryDate.ToString("dd/MM/yyyy")</span>
                                </td>
                                <td class="text-center">
                                    <span class="text-muted small">@coupon.UsedCount / @coupon.UsageLimit</span>
                                    <div class="progress mt-1" style="height:4px; width:80px; margin:auto;">
                                        @{
                                            var pct = coupon.UsageLimit > 0 ? Math.Min(100, (int)(coupon.UsedCount * 100.0 / coupon.UsageLimit)) : 0;
                                        }
                                        <div class="progress-bar @(pct >= 100 ? "bg-danger" : "bg-success")" style="width:@pct%"></div>
                                    </div>
                                </td>
                                <td class="text-center">
                                    @if (expired)
                                    {
                                        <span class="badge bg-danger bg-opacity-10 text-danger border border-danger-subtle px-2 py-1 rounded-pill">Hết hạn</span>
                                    }
                                    else if (coupon.UsedCount >= coupon.UsageLimit)
                                    {
                                        <span class="badge bg-warning bg-opacity-10 text-warning border border-warning-subtle px-2 py-1 rounded-pill">Đã dùng hết</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success bg-opacity-10 text-success border border-success-subtle px-2 py-1 rounded-pill">Còn hiệu lực</span>
                                    }
                                </td>
                                <td class="px-4 text-end">
                                    <a asp-area="Admin" asp-controller="Coupon" asp-action="Edit" asp-route-id="@coupon.Id"
                                       class="btn btn-sm btn-light text-primary border shadow-sm rounded-3 me-1" title="Sửa">
                                        <i class="bi bi-pencil-square"></i>
                                    </a>
                                    <form asp-area="Admin" asp-controller="Coupon" asp-action="Delete" method="post" class="d-inline"
                                          onsubmit="return confirm('Xóa mã giảm giá này?');">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@coupon.Id" />
                                        <button type="submit" class="btn btn-sm btn-light text-danger border shadow-sm rounded-3" title="Xóa">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" class="text-center py-5 text-muted">
                                <div class="fs-1 mb-3">🎟️</div>
                                <h5>Chưa có mã giảm giá nào</h5>
                                <p>Hãy thêm mã giảm giá đầu tiên!</p>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <div class="card-footer bg-white border-top p-3 d-flex justify-content-between align-items-center">
        <span class="text-muted small">Hiển thị @Model?.Count() mã giảm giá</span>
    </div>
</div>

